<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FinansCepte.AnalysisPage"
             Title="Analiz"
              BackgroundColor="#F5F5F5">
    
    <ScrollView>
        <VerticalStackLayout Spacing="20" Padding="20">
            <Border StrokeShape="RoundRectangle 15"
                    BackgroundColor="Black"
                    Padding="20"
                    StrokeThickness="0">
                <VerticalStackLayout Spacing="10">
                    <Label Text="Aylık Bütçe Kullanımı" TextColor="LightGray" FontSize="14" HorizontalOptions="Center"/>
                    <Label x:Name="LblPercentage" Text="%0" TextColor="White" FontSize="40" FontAttributes="Bold" HorizontalOptions="Center"/>
                    <Label x:Name="LblRemaining" Text="Hesaplanıyor..." TextColor="#4CAF50" FontSize="14" HorizontalOptions="Center"/>
                    <ProgressBar x:Name="PbLimit" Progress="0" ProgressColor="#4CAF50" ScaleY="2" Margin="0,10,0,0"/>
                </VerticalStackLayout>
            </Border>
        
        <Label Text="Kategori Dağılımı" FontAttributes="Bold" FontSize="18" TextColor="Black" Margin="5,0,0,0"/>
        
        <CollectionView x:Name="CvCategories" HeightRequest="300">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <VerticalStackLayout Spacing="5" Margin="0,0,0,15">
                        <Border StrokeShape="RoundRectangle 10" Padding="15" StrokeThickness="0" BackgroundColor="White">
                            <VerticalStackLayout Spacing="5">
                                <Grid ColumnDefinitions="*, Auto">
                                    <Label Grid.Column="0" Text="{Binding Category}" FontAttributes="Bold" TextColor="#333"/>
                                    <Label Grid.Column="1" Text="{Binding TotalAmount, StringFormat='{0:N0} ₺'}" TextColor="Gray"/>
                                </Grid>
                                <ProgressBar Progress="{Binding Percentage}" ProgressColor="{Binding ColorCode}" ScaleY="1.5"/>
                            </VerticalStackLayout>
                        </Border>
                    </VerticalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        
            <Border StrokeShape="RoundRectangle 10" Padding="15" StrokeThickness="0" BackgroundColor="White" Margin="0,10,0,20">
                <HorizontalStackLayout Spacing="15" VerticalOptions="Center">
                    <Label Text="💡" FontSize="24" VerticalOptions="Center"/>
                    <Label x:Name="LblTip" 
                           Text="Veri bekleniyor..." 
                           TextColor="#E65100" 
                           FontSize="12" 
                           VerticalOptions="Center"
                           LineBreakMode="WordWrap"
                           MaximumWidthRequest="300"/> </HorizontalStackLayout>
            </Border>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>